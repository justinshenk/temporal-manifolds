setup:
  model: "Qwen/Qwen3-4B-Instruct-2507"
  seed: 0
  batch_size: 64
  dtype: "bfloat16"
  granularity: "fine"
  quadrature: "riemann-midpoint"

paths:
  data_loc: "./data/raw/refined_datasets"
  save_loc: "./results/implicit/(a_)"

input:
  data_file: "temporal_scope_implicit_expanded_500_debiased.json"
  template: "{}\n\n{}\n{}"
  option_keys: [ "(a)", "(b)" ]
  prompt_suffix: "("
  horizon: [ "ST", "LT" ]

output:
  filename: "(a)_implicit"

parameters:
  system_prompt: "Recommend an approach for the given scenario. Reply only with (a) or (b)"
  steps: [ 20 ]
  metric_type: "option-logit"
